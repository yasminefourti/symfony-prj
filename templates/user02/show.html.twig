{% extends 'base.html.twig' %}

{% block title %}User02{% endblock %}

{% block body %}
    <h1>User02</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user02.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user02.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user02.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user02.password }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ user02.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user02_index') }}">back to list</a>

    <a href="{{ path('app_user02_edit', {'id': user02.id}) }}">edit</a>

    {{ include('user02/_delete_form.html.twig') }}
{% endblock %}
